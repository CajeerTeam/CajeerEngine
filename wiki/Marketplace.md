# Marketplace

Marketplace использует пакет `package.zip` с `cajeer.json` или `extension.json`.

Installer проверяет manifest, тип, версию, permissions, path traversal, запрещённые расширения и регистрирует пакет в `cajeer_extensions`.
