# DLE Compatibility

Поддерживаются `.tpl`-теги, conditional blocks, безопасный `{include file="..."}` внутри темы и базовый `{custom ...}` через ContentRepository.

Scanner помечает элементы как `supported`, `partially_supported`, `dangerous` или `unsupported`.
