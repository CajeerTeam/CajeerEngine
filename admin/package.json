{
  "name": "@cajeer/cajeerengine-admin-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck",
    "build:release": "nuxt generate && node ./scripts/publish-admin-assets.mjs --check",
    "preview:static": "node ./scripts/publish-admin-assets.mjs --check",
    "build:static": "node ./scripts/publish-admin-assets.mjs --check"
  },
  "dependencies": {
    "nuxt": "4.2.0",
    "vue": "3.5.0"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "version": "1.1.1",
  "engines": {
    "node": ">=20.19.0"
  }
}
